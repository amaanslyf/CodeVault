# Dependency directories
# These are installed via 'npm install' and should not be in version control.
node_modules/
config/
# Environment variables
# This file contains sensitive secrets like database passwords and API keys.
.env

# Logs
# Log files are specific to a user's environment and can become very large.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local repository state created by the CodeVault CLI
# This directory is created by the 'init' command and holds user-specific state.
.codevault/

# IDE and editor directories
.idea
.vscode/
*.swp

# Optional: Coverage directory from testing frameworks
coverage/

# Optional: OS-generated files
.DS_Store
Thumbs.db
